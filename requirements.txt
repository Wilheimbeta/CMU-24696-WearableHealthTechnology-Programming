# =============================================================================
# Unified Requirements for HART, LIMU-BERT, and ssl-wearables
#
# Python 3.10 required  (TF 2.10 max = 3.10, PyTorch 2.7+ min = 3.10)
# GPU: RTX 50-series (Blackwell) needs PyTorch 2.7+ with CUDA 12.8
#
# ---- Setup (conda + pip) ----
#
#   Step 1: Create environment
#     conda create -n har python=3.10 -y
#     conda activate har
#
#   Step 2: Install PyTorch with CUDA 12.8 (for LIMU-BERT + ssl-wearables)
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
#   Step 3: Install TensorFlow CUDA libs (for HART GPU support on Windows)
#     conda install -c conda-forge cudatoolkit=11.8 cudnn=8.9.7 -y
#
#   Step 4: Install remaining dependencies
#     pip install -r requirements.txt
#
# ---- CPU-only alternative (no GPU) ----
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#     pip install -r requirements.txt
#
# ---- Verify GPU ----
#     python -c "import torch; print('PyTorch CUDA:', torch.cuda.is_available(), torch.cuda.get_device_name(0) if torch.cuda.is_available() else '')"
#     python -c "import tensorflow as tf; print('TF GPUs:', tf.config.list_physical_devices('GPU'))"
# =============================================================================

# --- TensorFlow (HART) ---
tensorflow==2.10.1

# --- PyTorch (LIMU-BERT + ssl-wearables) ---
# NOTE: Install PyTorch via --index-url BEFORE this file (see above).
#       The line below is only a minimum version check.
torch>=2.7.0
torchvision>=0.22.0

# --- Core Scientific ---
numpy>=1.21.0,<1.24.0
scipy>=1.7.0
scikit-learn>=1.0.0
pandas>=1.3.0

# --- Visualization ---
matplotlib>=3.5.0
seaborn>=0.12.0

# --- Data Formats ---
hickle>=5.0.0
h5py>=3.8.0

# --- Configuration (ssl-wearables uses Hydra) ---
hydra-core>=1.2.0
omegaconf>=2.2.0

# --- Imbalanced Learning (ssl-wearables) ---
imbalanced-learn>=0.9.0

# --- Utilities ---
tqdm
requests>=2.28.0
lmdb>=1.3.0
transforms3d>=0.3.1
